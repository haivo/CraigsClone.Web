@model HomeVm

<h1>CraigsClone</h1>

<p>pick a city:</p>
<ul class="cities">
    @foreach (var city in Model.Cities)
    {
        <li><a href="/@city.Slug">@city.Name</a></li>
    }
</ul>

<partial name="_CategoryGroups" model="new CategoryGroupsVm(Model.Groups, null)" />
